/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true,
  // swcMinify: true,
  env: {
    REACT_APP_API_URL: 'https://api.conectaedu.co',
  },
}

module.exports = nextConfig
